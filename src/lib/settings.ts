// Staleness timeout in seconds. A device is considered "stale" if its
// last_data_ts is older than this many seconds ago. Used to visually dim
// devices on the floorplan canvas.
export const STALE_TIMEOUT_SECONDS = 300; // 5 minutes
